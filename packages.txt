# Fedora packages for this Sway/i3-style desktop.
#
#   sudo dnf install -y --setopt=strict=0 $(grep -v '^#' packages.txt)
#
# git and neovim are assumed already installed on this machine.

sway
swaybg
swaylock
swayidle
waybar
wofi
mako
grim
slurp
wl-clipboard
brightnessctl
playerctl
pipewire
pipewire-alsa
pipewire-pulseaudio
wireplumber
alsa-utils
NetworkManager
polkit-gnome
xorg-x11-server-Xwayland
dejavu-sans-fonts
dejavu-sans-mono-fonts
xdg-desktop-portal
xdg-desktop-portal-wlr
st
unzip
dconf
gsettings-desktop-schemas

# ------------------------------------------------- simplified_nvim toolchain ---
# Not config, but simplified_nvim/README.md's LSP servers need these to build
# (blink.cmp's native matcher, nvim-treesitter parser compilation) or to
# install (rustup/npm are the delivery mechanism, not dnf packages).

gcc
make
nodejs
npm
rustup
clang-tools-extra
ripgrep
fd-find
lazygit
